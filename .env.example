# Environment Configuration Example
# Copy this file to .env.local for local development

# Deployment Target (automatically set by build scripts)
# DEPLOY_TARGET=development|github|production

# Production Environment Variables (Digital Ocean only)
# Uncomment and set these for production deployment

# API Configuration
# NEXT_PUBLIC_API_URL=https://api.your-domain.com

# Analytics (production only)
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id
# NEXT_PUBLIC_GTM_ID=your-google-tag-manager-id

# Database (if needed for production)
# DATABASE_URL=your-database-url

# External Services (if used)
# GITHUB_TOKEN=your-github-token
# LINKEDIN_API_KEY=your-linkedin-api-key

# Note: 
# - GitHub Pages deployment requires NO environment variables
# - Local development works without any environment variables
# - Only production deployment on Digital Ocean may need these variables
